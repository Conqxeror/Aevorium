flwr[simulation]>=1.5.0
# NOTE: Install PyTorch with CUDA support for GPU acceleration. On this machine, NVIDIA drivers report CUDA 13.0.
# Recommended install command for Windows (PowerShell):
#   pip uninstall -y torch torchvision torchaudio
#   pip install --index-url https://download.pytorch.org/whl/cu130 torch torchvision torchaudio --upgrade
# Leave a generic spec so non-GPU installs can still run. The user should install the proper "cu130" wheel.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0
fastapi>=0.100.0
uvicorn>=0.22.0
opacus>=1.4.0
matplotlib>=3.7.0
cryptography>=41.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Dashboard
streamlit>=1.28.0
plotly>=5.18.0
requests>=2.31.0
